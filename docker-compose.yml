services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      - petipro_data:/app
    environment:
      - PYTHONUNBUFFERED=1

  frontend:
    build: ./frontend
    ports:
      - "80:80"
    depends_on:
      - backend

volumes:
  petipro_data:
